# mulle-sde Crosscompiling
<!-- Keywords: crosscompile, windows, linux -->

Use `mulle-sde platform` to activate and deactivate cross-platform development.

You will need a suitable toolchain file in `cmake/` or `cmake/shared`.
Currently the documented path is Windows cross-compilation via
`mulle-clang-project-windows`.
