## 3.1.0








* include macro `MULLE__CONCURRENT_GLOBAL` is now defined when `MULLE__CORE_BUILD` is set, ensuring correct symbol visibility when embedding mulle-concurrent into a core build
* updated/added test examples (array, hashmap) and a test toolchain to validate integration
